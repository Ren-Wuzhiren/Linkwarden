# AGENTS.md — 项目协作与学习规范

> 本文件是本仓库与 Codex 协作的最高优先级约定；每次会话开始先读取并遵守。它记录约定和边界，不替代 PRD、设计文档与源码研读笔记。

## 项目概述

- 项目：Web 收藏与内容保存系统。
- 使用者：作者自己，后续可用真实使用记录支撑作品集。
- 目标：独立实现一个可自托管、可检索、可回看的网页保存系统。
- 学习目标：理解 Linkwarden 解决的真实问题和架构，再通过 Java/Vue 独立实现，而不是直接复制上游代码。

## 第一版边界

- 做：账号登录、扁平集合、标签、URL 保存、异步采集、HTML/PDF/截图、检索、详情与阅读视图。
- 不做：协作权限、公开分享、浏览器扩展、移动端、AI 标签、RSS、SSO、嵌套集合、MinIO、Meilisearch。
- 可靠性优先：MySQL 任务表作为持久化任务队列，Redis 用于锁、限流和缓存。
- 安全优先：URL 必须是 HTTP/HTTPS；保存前拦截 localhost、私网、云元数据地址；重定向后重新校验；限制超时和响应大小。

## 学习分工

1. **Codex 负责**：讲解架构与设计理由、拆解阶段任务、审查代码、编写前端、整理 PRD/工程化/排错/复盘文档。
2. **用户负责**：后端代码亲手实现；每个阶段先理解再编写；遇到不懂的调用链、并发、事务或协议细节随时要求展开。
3. **先讲后写**：开始一个阶段前，先输出目标、数据流、接口、边界和验收标准；用户确认后再进入实现。
4. **渐进式推进**：每次只推进一个可验收增量，不一次性写完整个系统。
5. **不替用户做决定**：涉及技术选型或影响学习目标的取舍时，列明利弊后由用户拍板。

## 13 步标准工作流

1. 用户对接。
2. 制作需求文档 / PRD。
3. 设计原型。
4. 可行性分析。
5. 绘制业务与系统流程图。
6. 技术选型。
7. 架构设计。
8. 数据库设计。
9. 接口文档。
10. 编码开发。
11. 审核检查。
12. 部署上线。
13. 验收交付。

小功能可以压缩步骤，但「需求 → 设计 → 编码 → 审核 → 验收」不可省。每一步的产出物必须落盘，不能只停留在聊天记录。

## 参考源码规则

- 上游源码位于 `reference/linkwarden/`，固定在标签 `v2.16.3`，该目录被 `.gitignore` 忽略。
- 不修改上游仓库，不把上游源码、Logo、截图或品牌资源提交到本项目。
- 独立重写可以借鉴问题定义、数据流和设计思路，但不得把上游代码直接翻译成 Java 后声称为原创实现。
- Linkwarden 采用 AGPL-3.0；任何代码复制、修改后分发或网络服务发布都要单独确认许可证义务。

## Git 规则

- 自有项目分支为 `main`。
- 每个完成并通过验收的阶段形成一个独立 commit。
- 不提交 `reference/`、`.env`、`.idea/`、运行数据、密钥或本地个人配置。
- 提交前运行与阶段对应的测试，并检查 `git diff --stat` 与 `git status`。

## 文档规范

- `docs/需求文档/`：PRD、阶段计划、决策记录和验收记录；文件名使用 `YYYY-MM-DD-NNN-主题.md`。
- `docs/工程化文档/`：原型、可行性、流程图、架构、数据库、接口和交付清单。
- `docs/源码研读/`：Linkwarden 的全景、调用链、数据模型、API 和取舍笔记。
- `docs/技术笔记.md`：可复用的技术原理与实现坑。
- `docs/排错笔记.md`：真实故障、现象、定位过程和修复结论。
- `docs/四大件笔记/`：从项目经历反哺数据结构与算法、计算机组成原理、操作系统、计算机网络。
- `docs/复盘/`：阶段复盘和最终项目总结。

## 验收原则

- 每个增量必须同时具备：可运行结果、测试或手工验证记录、文档更新、清晰的 Git commit。
- 不以“代码写完了”作为完成标准；只有核心链路跑通、失败路径被验证并且你能解释设计，才算完成。
- 第一版验收目标：30 个不同网页采集成功率不低于 90%，提交到快照完成的中位时间不超过 30 秒，核心保存与找回流程无需查阅文档即可完成。

